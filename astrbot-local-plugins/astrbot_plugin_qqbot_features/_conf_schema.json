{
  "feature_mode": {
    "description": "迁移功能响应模式",
    "type": "string",
    "default": "full",
    "options": [
      "dual",
      "full"
    ],
    "hint": "full: AstrBot 接管已迁移自动事件。dual 仅保留为旧配置兼容，运行时也按 full 处理。环境变量 QQBOT_ASTRBOT_FEATURE_MODE 会覆盖这里。"
  },
  "auto_approve_friend_requests": {
    "description": "自动同意好友申请",
    "type": "bool",
    "default": true,
    "hint": "收到 OneBot friend request 后调用 set_friend_add_request。"
  },
  "auto_approve_group_invites": {
    "description": "自动同意邀请入群",
    "type": "bool",
    "default": true,
    "hint": "只处理 request_type=group 且 sub_type=invite 的邀请入群事件，不处理普通加群申请。"
  },
  "data_root": {
    "description": "RightCodes 生图积分存储根目录",
    "type": "string",
    "default": "",
    "hint": "留空时自动使用工作区 data/nonebot2/run 的迁移期存档。"
  },
  "api_key": {
    "description": "RightCodes API Key",
    "type": "string",
    "default": "",
    "hint": "直接填写 RightCodes 生图 API Key；示例导出会脱敏，不再读取 NoneBot2 .env 或 QQBOT_AI_KEY_RIGHTCODES。"
  },
  "point_multiplier": {
    "description": "RightCodes 生图积分倍率",
    "type": "int",
    "default": 1000,
    "hint": "模型美元价格 x 倍率后向上取整。"
  },
  "draw_timeout_seconds": {
    "description": "RightCodes 生图总超时秒数",
    "type": "int",
    "default": 240,
    "hint": "生图通常超过 1 分钟；超过该时间仍未返回时，插件回复失败并退回本次扣除的积分或免费次数。"
  }
}
